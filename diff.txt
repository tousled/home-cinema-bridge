diff --git a/lib/Xnoppo.py b/lib/Xnoppo.py
index 6fedc03..b307b05 100755
--- a/lib/Xnoppo.py
+++ b/lib/Xnoppo.py
@@ -8,9 +8,10 @@ import time
 
 from .Xnoppo_AVR import av_check_power, av_power_off, av_change_hdmi
 from .Xnoppo_TV import tv_change_hdmi, tv_set_prev
-from lib.devices.oppo.http_client import OppoHttpClient
-from lib.devices.oppo.playback_startup import wait_until_active_playback
-from lib.devices.oppo.status_client import OppoStatusClient
+from lib.devices.oppo.control_api_client import OppoControlApiClient
+from lib.devices.oppo.playback_state_waiter import wait_until_oppo_reports_active_playback
+from lib.devices.oppo.playback_status_client import OppoPlaybackStatusClient
+from .devices.oppo.control_api_activation import OppoControlApiActivator
 from .oppo_autoscript import umount_shared_folder
 
 _qpl_last_observed_states = {}
@@ -30,7 +31,7 @@ def log_oppo_qpl_state(config, label, changes_only=False):
             print(f"QPL:{label} skipped | Oppo_IP is not configured")
             return None
 
-        client = OppoStatusClient(
+        client = OppoPlaybackStatusClient(
             host=oppo_ip,
             port=int(config.get("OPPO_Port", 23)),
             timeout=float(config.get("oppo_control_api_check_timeout", 3)),
@@ -91,8 +92,8 @@ def log_oppo_qpl_state_sequence(config, label, samples=5, interval=1):
             pass
 
 
-def oppo_http_client(config):
-    return OppoHttpClient.from_config(config)
+def oppo_control_api_client(config):
+    return OppoControlApiClient.from_config(config)
 
 
 def sendnotifyremote(UDP_IP):
@@ -108,49 +109,43 @@ def sendnotifyremote(UDP_IP):
 
     return 0
 
-def check_socket(config,session_id=None):
-    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
-    logging.debug('Comprobando apertura del puerto del OPPO ')
-    result = sock.connect_ex((config["Oppo_IP"],436))
-    logging.debug("Resultado Chequeo: %s",str(result))
-    net_retries=config["oppo_control_api_check_timeout"]
-    net_wait=0
-    while result > 0 and net_wait<net_retries:
-              time.sleep(1)
-              net_wait=net_wait+1
-              logging.info('Esperando apertura del puerto del OPPO')
-              logging.info( 'Reintento %s',str(net_wait))
-              sendnotifyremote(config["Oppo_IP"])
-              result = sock.connect_ex((config["Oppo_IP"],436))
-    if net_wait>=net_retries:
-            logging.debug('Timeout esperando puerto del OPPO')
-            return(1)
-    else:
-            logging.debug('Puerto del OPPO abierto')
-            return(0)
+def check_socket(config, session_id=None):
+    activator = OppoControlApiActivator.from_config(config)
+    result = activator.ensure_control_api_available(
+        max_attempts=int(config["oppo_control_api_check_timeout"])
+    )
+
+    if result.available:
+        logging.debug("OPPO control API available | host=%s | port=%s | attempts=%s",
+                      result.host, result.port, result.attempts)
+        return 0
+
+    logging.warning("Timeout waiting for OPPO control API | host=%s | port=%s | attempts=%s | error=%s",
+                    result.host, result.port, result.attempts, result.error)
+    return 1
 
 def getmainfirmwareversion(config):
-    return oppo_http_client(config).get_main_firmware_version()
+    return oppo_control_api_client(config).get_main_firmware_version()
 
 
 def getsetupmenu(config):
-    return oppo_http_client(config).get_setup_menu()
+    return oppo_control_api_client(config).get_setup_menu()
 
 
 def OppoSignin(config):
-    return oppo_http_client(config).sign_in()
+    return oppo_control_api_client(config).sign_in()
 
 
 def getdevicelist(config):
-    return oppo_http_client(config).get_device_list()
+    return oppo_control_api_client(config).get_device_list()
 
 
 def getglobalinfo(config):
-    return oppo_http_client(config).get_global_info()
+    return oppo_control_api_client(config).get_global_info()
 
 
 def getplayingtime(config):
-    return oppo_http_client(config).get_playing_time()
+    return oppo_control_api_client(config).get_playing_time()
 
 def mountSharedFolderID(server, folder, Username, Password, config):
     if config["DebugLevel"] == 2:
@@ -158,7 +153,7 @@ def mountSharedFolderID(server, folder, Username, Password, config):
 
     logging.debug("*** mountSharedFolderID ***")
 
-    response_text = oppo_http_client(config).mount_samba_folder_with_id(
+    response_text = oppo_control_api_client(config).mount_samba_folder_with_id(
         server=server,
         folder=folder,
         username=Username,
@@ -181,7 +176,7 @@ def mountSharedFolder(server, folder, Username, Password, config, checksmb=True)
     if config["smbtrick"] is True and checksmb is True:
         smbtrick(server + "/" + folder, config)
 
-    response_text = oppo_http_client(config).mount_samba_folder(
+    response_text = oppo_control_api_client(config).mount_samba_folder(
         server=server,
         folder=folder,
         timeout=config["timeout_oppo_mount"],
@@ -200,7 +195,7 @@ def mountSharedNFSFolder(server, folder, Username, Password, config):
 
     logging.debug("*** mountSharedNFSFolder ***")
 
-    response_text = oppo_http_client(config).mount_nfs_folder(
+    response_text = oppo_control_api_client(config).mount_nfs_folder(
         server=server,
         folder=folder,
         timeout=config["timeout_oppo_mount"],
@@ -215,7 +210,7 @@ def mountSharedNFSFolder(server, folder, Username, Password, config):
 
 def LoginNFS(config, server):
     logging.debug("LoginNFS")
-    response_text = oppo_http_client(config).login_nfs_server(server)
+    response_text = oppo_control_api_client(config).login_nfs_server(server)
 
     if config["DebugLevel"] == 2:
         print("*** LoginNFS Response: " + response_text)
@@ -230,7 +225,7 @@ def playnormalfile(server, filename, index, config, nfs):
 
     logging.debug("*** playnormalfile ***")
 
-    response_text = oppo_http_client(config).play_normal_file(
+    response_text = oppo_control_api_client(config).play_normal_file(
         server=server,
         filename=filename,
         index=index,
@@ -251,7 +246,7 @@ def checkfolderhasbdmv(config, folder, nfs):
 
     logging.debug("*** checkfolderhasbdmv ***")
 
-    response_text = oppo_http_client(config).check_folder_has_bdmv(
+    response_text = oppo_control_api_client(config).check_folder_has_bdmv(
         folder=folder,
         nfs=nfs,
         timeout=config["timeout_oppo_playitem"],
@@ -587,7 +582,7 @@ def setaudiotrack(config,audio_index):
 
 def LoginSambaWithOutID(config, server):
     logging.debug("LoginSambaWithOutID")
-    response_text = oppo_http_client(config).login_samba_without_id(server)
+    response_text = oppo_control_api_client(config).login_samba_without_id(server)
 
     if config["DebugLevel"] == 2:
         print("*** LoginSambaWithOutID Response: " + response_text)
@@ -822,7 +817,7 @@ def playother(EmbySession,data,scripterx=False):
                 999,
             )
 
-    startup_result = wait_until_active_playback(
+    startup_result = wait_until_oppo_reports_active_playback(
         config=EmbySession.config,
         timeout=timeout,
         interval=0.5,
diff --git a/lib/devices/oppo/control_api_activation.py b/lib/devices/oppo/control_api_activation.py
index ad0f7ff..0c200dd 100644
--- a/lib/devices/oppo/control_api_activation.py
+++ b/lib/devices/oppo/control_api_activation.py
@@ -1,53 +1,100 @@
+import logging
 import socket
+import time
 from dataclasses import dataclass
 
 
-OPPO_HTTP_PORT = 436
+OPPO_CONTROL_API_PORT = 436
+OPPO_REMOTE_LOGIN_PORT = 7624
+OPPO_REMOTE_LOGIN_MESSAGE = "NOTIFY OREMOTE LOGIN"
 
 
 @dataclass(frozen=True)
-class OppoAvailability:
+class OppoControlApiActivationResult:
     available: bool
     host: str
     port: int
+    attempts: int
     error: str = ""
 
 
-def check_oppo_http_availability(
-    config: dict,
-    *,
-    timeout: float = 0.5,
-) -> OppoAvailability:
+class OppoControlApiActivator:
     """
-    Lightweight OPPO availability check.
+    Prepares the OPPO/Chinoppo HTTP control API.
 
-    This only checks whether the OPPO/Chinoppo HTTP control port is reachable.
-    It does not send wake/login packets and it does not retry. Use this for
-    passive health checks such as the watchdog.
+    The OPPO can respond to ping and QPL/QPW while the HTTP control API
+    on port 436 is still closed. Sending the legacy remote login NOTIFY
+    packet activates that HTTP control API.
     """
-    host = str(config.get("Oppo_IP", "")).strip()
-    port = OPPO_HTTP_PORT
 
-    if not host:
-        return OppoAvailability(
-            available=False,
-            host="",
-            port=port,
-            error="Oppo_IP is not configured",
+    def __init__(
+        self,
+        host: str,
+        *,
+        control_api_port: int = OPPO_CONTROL_API_PORT,
+        remote_login_port: int = OPPO_REMOTE_LOGIN_PORT,
+        timeout_seconds: float = 1.0,
+    ):
+        self.host = host
+        self.control_api_port = control_api_port
+        self.remote_login_port = remote_login_port
+        self.timeout_seconds = timeout_seconds
+
+    @classmethod
+    def from_config(cls, config: dict) -> "OppoControlApiActivator":
+        return cls(
+            host=str(config["Oppo_IP"]),
+            control_api_port=int(config.get("OPPO_HTTP_Port", OPPO_CONTROL_API_PORT)),
+            timeout_seconds=float(config.get("oppo_control_api_check_timeout", 3)),
         )
 
-    try:
-        with socket.create_connection((host, port), timeout=timeout):
-            return OppoAvailability(
-                available=True,
-                host=host,
-                port=port,
-            )
+    def is_control_api_available(self) -> bool:
+        try:
+            with socket.create_connection(
+                (self.host, self.control_api_port),
+                timeout=self.timeout_seconds,
+            ):
+                return True
+        except OSError:
+            return False
+
+    def ensure_control_api_available(self, *, max_attempts: int) -> OppoControlApiActivationResult:
+        last_error = ""
+
+        for attempt in range(1, max_attempts + 1):
+            if self.is_control_api_available():
+                return OppoControlApiActivationResult(
+                    available=True,
+                    host=self.host,
+                    port=self.control_api_port,
+                    attempts=attempt,
+                )
 
-    except OSError as exc:
-        return OppoAvailability(
+            try:
+                self._send_remote_login_notification()
+            except OSError as exc:
+                last_error = f"{type(exc).__name__}: {exc}"
+                logging.debug(
+                    "Failed to send OPPO remote login notification | attempt=%s | error=%s",
+                    attempt,
+                    last_error,
+                )
+
+            time.sleep(1)
+
+        return OppoControlApiActivationResult(
             available=False,
-            host=host,
-            port=port,
-            error=f"{type(exc).__name__}: {exc}",
+            host=self.host,
+            port=self.control_api_port,
+            attempts=max_attempts,
+            error=last_error or "Control API did not become available",
         )
+
+    def _send_remote_login_notification(self) -> None:
+        logging.debug("Sending OPPO remote login notification to %s:%s", self.host, self.remote_login_port)
+
+        with socket.socket(socket.AF_INET, socket.SOCK_DGRAM) as sock:
+            sock.sendto(
+                OPPO_REMOTE_LOGIN_MESSAGE.encode("utf-8"),
+                (self.host, self.remote_login_port),
+            )
\ No newline at end of file
diff --git a/lib/devices/oppo/control_api_client.py b/lib/devices/oppo/control_api_client.py
index 545e268..d652c2e 100644
--- a/lib/devices/oppo/control_api_client.py
+++ b/lib/devices/oppo/control_api_client.py
@@ -9,12 +9,12 @@ OPPO_HTTP_PORT = 436
 
 
 @dataclass(frozen=True)
-class OppoHttpClient:
+class OppoControlApiClient:
     host: str
     port: int = OPPO_HTTP_PORT
 
     @classmethod
-    def from_config(cls, config: dict) -> "OppoHttpClient":
+    def from_config(cls, config: dict) -> "OppoControlApiClient":
         return cls(
             host=str(config["Oppo_IP"]),
             port=int(config.get("OPPO_HTTP_Port", OPPO_HTTP_PORT)),
diff --git a/lib/devices/oppo/playback_state_waiter.py b/lib/devices/oppo/playback_state_waiter.py
index 5b4ee21..36d0146 100644
--- a/lib/devices/oppo/playback_state_waiter.py
+++ b/lib/devices/oppo/playback_state_waiter.py
@@ -17,7 +17,7 @@ class PlaybackStartupWaitResult:
     raw_response: str
 
 
-def wait_until_active_playback(
+def wait_until_oppo_reports_active_playback(
     config: dict,
     timeout: int | float,
     interval: float = 0.5,
diff --git a/lib/devices/oppo/playback_status_client.py b/lib/devices/oppo/playback_status_client.py
index 52b2933..8dfa1e2 100644
--- a/lib/devices/oppo/playback_status_client.py
+++ b/lib/devices/oppo/playback_status_client.py
@@ -18,15 +18,13 @@ class OppoCommandResult:
     category: OppoPlaybackCategory
 
 
-class OppoStatusClient:
-    """
-    Minimal OPPO/Chinoppo TCP status client.
-
-    The player accepts commands like:
-      #QPW\r -> @OK ON
-      #QPL\r -> @OK PLAY / @OK HOME MENU / @OK DISC MENU / ...
+class OppoPlaybackStatusClient:
+    """"
+    Reads OPPO playback/power status using the OPPO TCP command interface.
 
-    This client only reads status. It does not change playback state.
+    Examples:
+      #QPW -> @OK ON
+      #QPL -> @OK PLAY / @OK HOME MENU / @OK DISC MENU / ...
     """
 
     def __init__(self, host: str, port: int = 23, timeout: float = 3.0):
diff --git a/tools/oppo_qpl_probe.py b/tools/oppo_qpl_probe.py
index f9c45d0..2c4fda1 100644
--- a/tools/oppo_qpl_probe.py
+++ b/tools/oppo_qpl_probe.py
@@ -9,7 +9,7 @@ from pathlib import Path
 PROJECT_ROOT = Path(__file__).resolve().parent.parent
 sys.path.insert(0, str(PROJECT_ROOT))
 
-from lib.devices.oppo.status_client import OppoStatusClient
+from lib.devices.oppo.playback_status_client import OppoPlaybackStatusClient
 
 
 def now() -> str:
@@ -35,7 +35,7 @@ def print_result(result, previous_status: str | None) -> str:
     return result.status
 
 
-def run_once(client: OppoStatusClient, commands: list[str]) -> int:
+def run_once(client: OppoPlaybackStatusClient, commands: list[str]) -> int:
     for command in commands:
         try:
             result = client.query(command)
@@ -47,7 +47,7 @@ def run_once(client: OppoStatusClient, commands: list[str]) -> int:
 
 
 def run_watch(
-    client: OppoStatusClient,
+    client: OppoPlaybackStatusClient,
     command: str,
     interval: float,
     changes_only: bool,
@@ -107,7 +107,7 @@ def main() -> int:
     )
 
     args = parser.parse_args()
-    client = OppoStatusClient(host=args.host, port=args.port, timeout=args.timeout)
+    client = OppoPlaybackStatusClient(host=args.host, port=args.port, timeout=args.timeout)
 
     if args.watch:
         return run_watch(
